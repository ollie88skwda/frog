# Local dev: run `supabase start` at the repo root, then copy the values
# from `supabase status` here (API URL + anon key). Save as .env.local.
VITE_SUPABASE_URL=http://127.0.0.1:54321
VITE_SUPABASE_ANON_KEY=
